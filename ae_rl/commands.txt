# Phase 2: switch validation to held-out so best-checkpoint promotion
  # starts optimising for generalisation
  uv run python ae_rl/train_stage3_league.py \
    --updates 400 --critic-warmup 0 --lr 5e-5 \
    --episodes-per-update 24 --seq-minibatch 12 --epochs 3 \
    --snapshot-every 50 --gated-snapshots --snapshot-margin 25 \
    --validate-every 5 --validation-rounds 6 --validation-learners 1 \
    --validation-baseline vanilla \
    --rollback-on-regress --rollback-margin 35 \
    -j 24

  # Phase 3: polish phase, no shaping, raw env reward, low LR
  uv run python ae_rl/train_stage3_league.py \
    --updates 300 --critic-warmup 8 --lr 3e-5 \
    --no-shaping \
    --episodes-per-update 24 --seq-minibatch 12 --epochs 3 \
    --snapshot-every 0 --gated-snapshots --snapshot-margin 20 \
    --validate-every 5 --validation-rounds 6 --validation-learners 1 \
    --validation-baseline vanilla \
    --rollback-on-regress --rollback-margin 20 \
    -j 24

