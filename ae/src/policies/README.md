THIS FILE IS GENERATED BY CLAUDE FABLE 5

# Archived AE policy snapshots

This package replaces the old nested snapshot trees `azbase_preserved/`,
`azbase_preserved(0.724)/`, `azbasev3_preserved(0.748)/` and
`azbasev4_preserved/`. Those trees were full Docker-workspace copies; after
hash comparison, everything in them was either byte-identical to the live
`ae/src` top level or one of the five distinct policy files now kept here.

## Archive modules

| Module | Source (deleted tree) | Class |
| --- | --- | --- |
| `azbase_edited_policy.py` | `azbase_preserved/edited_policy.py` | `EditedHeuristicPolicy` |
| `azbase_edited_policy_v2.py` | `azbase_preserved/edited_policy_v2.py` | `EditedHeuristicPolicyV2` |
| `azbase_berserker_base_policy.py` | `azbase_preserved/berserker_base_azbase_policy.py` | `BerserkerBasePolicy` |
| `azbasev3_policy.py` | `azbasev3_preserved(0.748)/berserker_base_policy.py` | `BerserkerBasePolicy` |
| `azbasev4_policy.py` | `azbasev4_preserved/berserker_base_policy.py` | `BerserkerBaseV4Policy` (renamed from `BerserkerBasePolicy`) |

The only edits made while unfolding were import re-points
(`azbase_preserved.edited_policy*` / `.edited_policy*` →
`policies.azbase_edited_policy*`) and the provenance docstrings. Support
modules (`constants`, `map_memory`, `observation`, `pathfinding`, `policy`,
`threat`, `ideas.txt`, `novice_map.json`) were hash-identical across every
snapshot and the live `ae/src`, so the live top-level copies remain the
single source.

One post-unfold deviation from snapshot fidelity: the legacy learned-attack
stack (`rl_attack.py`, `rl_attack_model.py`, `rl_features.py`,
`rl_policy.py`) was deleted from the repo, so `azbase_edited_policy.py` (and
the live `edited_policy.py`) had their `from rl_attack import AttackModule`
import dropped and the `attack_module` constructor parameter loosened to
`Optional[object]`. The `attack_module is not None` hook paths are otherwise
untouched and the parameter defaults to `None`, so runtime behaviour is
unchanged. The images themselves shipped `rl_attack.py`, so a byte-faithful
reconstruction would need it back from git history (deleted after commit
`67f0eb6`).

## Live modules (folded from the `ae/src` top level)

The remaining top-level policy implementations were folded in afterwards;
these are the current working revisions, not snapshots:

| Module | Class | Notes |
| --- | --- | --- |
| `edited_policy.py` | `EditedHeuristicPolicy` | live revision; newer than `azbase_edited_policy.py` |
| `edited_policy_v2.py` | `EditedHeuristicPolicyV2` | live revision; newer than `azbase_edited_policy_v2.py` |
| `berserker_policy.py` | `BerserkerPolicy` | |
| `berserker_base_submit_policy.py` | `BerserkerBaseSubmitPolicy` | builds on the live `edited_policy*` |
| `scoremax_policy.py` | `ScoreMaxPolicy` | subclasses `azbasev3_policy.BerserkerBasePolicy` |

`azbasev3_policy.py` doubles as the **live submission policy** — the former
top-level `berserker_base_policy.py` was byte-identical to the v3 snapshot
and was deduplicated into the archive module; `ae_manager` imports
`HeuristicPolicy` from it. Fork before diverging the live policy from the
snapshot. `policy.py` (the `Policy` base class and original
`HeuristicPolicy`) stays at the `ae/src` top level as shared infrastructure.

Deduplicated (deleted) during the fold:

- `berserker_base_policy.py` — byte-identical to `azbasev3_policy.py`.
- `berserker_base_v4_policy.py` — same content as `azbasev4_policy.py`.
- `berserker_base_azbase_policy.py` — unimported dead code; same body as
  `azbase_berserker_base_policy.py` but its absolute imports bound it to the
  *live* `edited_policy*` modules instead of the azbase snapshots, so it was
  neither a faithful archive nor a used live module.

## Image provenance and score notes

### `overflow-ae:azbase` — best 0.724

Policy stack: `azbase_edited_policy` → `azbase_edited_policy_v2` →
`azbase_berserker_base_policy`. The image ran these as flat top-level
modules with absolute imports; the manager imported
`berserker_base_policy.BerserkerBasePolicy` (the file now archived as
`azbase_berserker_base_policy.py`).

Evaluator submissions: 0.663, 0.694, 0.665, 0.702, 0.724, 0.720, 0.661,
0.720, 0.713.

### `overflow-ae:azbasev3` — best 0.754

- Local tag: `overflow-ae:azbasev3`
- Remote tag: `asia-southeast1-docker.pkg.dev/til-ai-2026/repo-til-26-overflow/overflow-ae:azbasev3`
- Image ID: `sha256:82ab1076fd550ec6196e505ad0f815cfc4cbd76501ec317aaaebc1b328a5326c`

Active policy: `azbasev3_policy.py` (`BerserkerBasePolicy`), wired in
`ae_manager.py` via the restored-azbase import (sha256 `fe769b9e…`). This
module is also the current live submission policy — see "Live modules"
above.

Evaluator submissions: 0.684, 0.716, 0.748, 0.731, 0.748, 0.699, 0.754.

### `overflow-ae:azbasev4` — best 0.724 (pre-v5 candidate)

- Local tag: `overflow-ae:azbasev4`
- Remote tag: `asia-southeast1-docker.pkg.dev/til-ai-2026/repo-til-26-overflow/overflow-ae:azbasev4`
- Image ID: `sha256:ce4c03bc0b0b4029f98b3febd93e521740db5b9f19343c5dc3fe2e4e8785434c`

`azbasev4_policy.py` adds collected-tile cooldown / dead-base filtering on
top of the v3 stack; it does **not** include the later v5 emergency-defense
and position-churn changes. The manager pointed at
`berserker_base_policy.BerserkerBasePolicy` (the v4 variant of that file,
sha256 `93506d9a…`, archived here).

Evaluator submissions: 0.718, 0.724.

Source hashes recorded in the original v4 archive README ("from the Docker
image"):

- `ae_manager.py`: `8dcb7f5ade871c87200324957a14e190b1fb27116c760c6026b1b4ff84cabb43`
- `berserker_base_policy.py`: `93506d9a1109f1f3cbd44015248c070fba2dd9c8cac9675c9ed4a5c931d0ebbf`
- `azbase_preserved/edited_policy.py`: `227db2f2f4368966e39bb69fcd440bb59e90cc44d4abb0347feb8ec6456d080a`
- `azbase_preserved/edited_policy_v2.py`: `943f6f5b29d31de1a86717c5b8d0e1e3cb728974c79197adc31355293ec0e55a`
- `azbase_preserved/berserker_base_azbase_policy.py`: `f3a53438c702df0964769943c0500ad4fc99c4e4d28cf58cf0826bf949a51963`

**Hash caveat:** of those five, only `berserker_base_policy.py` (`93506d9a…`)
and `berserker_base_azbase_policy.py` (`f3a53438…`) match the bytes that were
in the repo snapshot trees. The repo's `edited_policy.py` /
`edited_policy_v2.py` hashed `abf6f06f…` / `7bf9e527…` (and the snapshot
`ae_manager.py` matched the live manager, `20c78c0c…`) — this was already
true at commit `67f0eb6` ("qualifiers"), so the archived bytes and the
README-recorded image hashes disagreed before the unfold. The modules in
this package carry the repo-snapshot bytes; the image-recorded hashes above
are kept in case the original images are ever needed for forensics.

## Dropped during the unfold

- `raw/` copies in the azbase trees — identical to the package files except
  absolute-vs-relative imports (`raw/edited_policy_v2.py` `a68cf4f6…`,
  `raw/berserker_base_policy.py` `9f532cc0…` — the latter byte-identical to
  the former top-level `berserker_base_azbase_policy.py`, deleted as dead
  code during the live-module fold).
- All nested duplicate trees inside `azbasev4_preserved/` (byte-identical to
  their top-level counterparts, modulo a trimmed README).
- v3's archive helpers `auto_play.py` (superseded by
  `ae/test_env/auto_play.py`) and `requirements.txt` (image artifact), plus
  the v3 `ae_manager.py` (`4ef2323e…`), whose only diff from the live
  manager was which `HeuristicPolicy` import line was commented out.
